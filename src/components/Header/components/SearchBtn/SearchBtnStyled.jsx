import styled from 'styled-components'

const SearchBtnWrapper = styled.a`
  padding: 0 0.75rem;
  font-size: 1.25rem;
  line-height: 1.75rem;
  letter-spacing: 3.5px;
  transition: all 0.15s ease-in-out;
  cursor: pointer;
  opacity: 0.7;

  &:hover {
    opacity: 0.4;
  }
`

export { SearchBtnWrapper }
